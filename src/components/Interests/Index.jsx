import React, { memo, useEffect, useRef } from "react";
import { InterestsContainer } from "./Interests.styles";

function Contact() {
	return <InterestsContainer id="interests" />;
}

export default memo(Contact);
